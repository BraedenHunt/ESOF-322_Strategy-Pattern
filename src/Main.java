public class Main {

    /*
     To compile: navigate to the directory of Main.java and run
                 the command: javac Main.java

      To run: Navigate to the directory of Main.class (created
              by the compiler) and run the command: java Main1
     */
    public static void main(String[] args) {
        Client client = new Client();
        // Run the client showcase
        client.run();
    }
}
